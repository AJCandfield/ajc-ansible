---
- name: Updates cache and upgrades system
  become: true
  apt:
    update_cache: true
    install_recommends: true
    upgrade: true
    autoremove: true
    autoclean: true

- name: Installs basic packages
  become: true
  apt:
    package: "{{ packages }}"
