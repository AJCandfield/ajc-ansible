---
# tasks file for roles/system-update
- name: Importing other tasks...
  loop:
    - install_base_packages
    - install_extra_packages
    - install_pip_packages

  include_tasks: "{{ item }}.yml"
